import { styled, Box } from '@mui/material';

export const ImageContainer = styled(Box)(() => ({
  borderRadius: '16px',
  height: '424px',
  position: 'relative',
  margin: '0 auto',
  width: '424px',

  img: {
    borderRadius: '16px'
  }
}));
