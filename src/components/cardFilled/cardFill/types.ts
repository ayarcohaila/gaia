export interface CardFillProps {
  card: {
    config: {
      avatar: string;
      collectionName: string;
      collectionSize: string;
      comingStatus: string;
      mainColor: string;
      ipLogo: string;
      ipTextColor: string;
      pageTitle: string;
    };

    lowerPrice: number;
  };
}
