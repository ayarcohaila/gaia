import { Box, Typography } from '@mui/material';

const BlockchainHistory = () => {
  return (
    <Box>
      <Typography>BlockchainHistory</Typography>
    </Box>
  );
};

export default BlockchainHistory;
