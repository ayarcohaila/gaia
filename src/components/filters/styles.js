import { styled } from '@mui/material';

import { Button } from '~/base';

export const FloatButton = styled(Button)({
  bottom: '24px',
  left: '50%',
  marginLeft: '-75px',
  position: 'fixed',
  width: '150px'
});
